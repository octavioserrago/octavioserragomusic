---
import Hero from "../components/Hero.astro";
import Music from "../components/Music.astro";
import Footer from "../components/Footer.astro";
import "../styles/global.css";
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta
            name="description"
            content="Official page of Octavio Serrago, country and bluegrass artist"
        />
        <title>Inicio</title>
        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css"
            integrity="sha512-DxV+EoADOkOygM4IR9yXP8Sb2qwgidEmeqAEmDKIOfPRQZOWbXCzLC6vjbZyy0vPisbH2SyW27+ddLVCN+OMzQ=="
            crossorigin="anonymous"
            referrerpolicy="no-referrer"
        />
    </head>
    <body class="font-sans text-white scroll-smooth relative">
        <!-- Fondo con overlay -->
        <div class="fixed inset-0 -z-10">
            <img
                src="/fotolanding.jpg"
                alt="Fondo"
                class="w-full h-full object-cover"
            />
            <div class="absolute inset-0 bg-black/30"></div>
        </div>

        <!-- Hero Section -->
        <Hero />

        <!-- Story Section -->
        <section class="mx-auto flex w-full max-w-5xl flex-col gap-8 px-6 py-16 text-white">
            <div class="grid gap-8 lg:grid-cols-3">
                <div class="rounded-3xl border border-white/10 bg-white/5 p-6 backdrop-blur">
                    <p class="text-xs uppercase tracking-[0.4em] text-amber-200">
                        Manifiesto sonoro
                    </p>
                    <h3 class="mt-3 text-2xl font-semibold">
                        Corridos criollos con pulso cinematográfico.
                    </h3>
                    <p class="mt-3 text-sm text-white/70">
                        Banjos, slide guitars y percusiones mínimas mezclan country clásico con poesía pampeana para contar historias nocturnas y de carretera.
                    </p>
                </div>
                <div class="rounded-3xl border border-white/10 bg-white/5 p-6 backdrop-blur">
                    <p class="text-xs uppercase tracking-[0.4em] text-amber-200">
                        En vivo
                    </p>
                    <h3 class="mt-3 text-2xl font-semibold">
                        Shows íntimos y festivales federales.
                    </h3>
                    <p class="mt-3 text-sm text-white/70">
                        Más de 120 presentaciones entre Buenos Aires y el interior, fogonazos acústicos, ferias camperas y noches a la intemperie junto a nuevas voces del folk.
                    </p>
                </div>
                <div class="rounded-3xl border border-white/10 bg-white/5 p-6 backdrop-blur">
                    <p class="text-xs uppercase tracking-[0.4em] text-amber-200">
                        En el estudio
                    </p>
                    <h3 class="mt-3 text-2xl font-semibold">
                        Produciendo el próximo disco.
                    </h3>
                    <p class="mt-3 text-sm text-white/70">
                        Actualmente trabajando en un álbum de estudio para 2025, con arreglos analógicos y colaboraciones sorpresa. Escribime si querés sumarte al proceso.
                    </p>
                </div>
            </div>
        </section>

        <!-- Music Section -->
        <Music />

        <!-- Contact CTA -->
        <section class="px-6 pb-20">
            <div class="mx-auto flex w-full max-w-5xl flex-col gap-6 rounded-3xl border border-white/10 bg-white/5 p-8 text-center text-white backdrop-blur">
                <p class="text-xs font-semibold uppercase tracking-[0.4em] text-amber-200">
                    Booking & prensa
                </p>
                <h3 class="text-3xl font-bold">
                    ¿Querés llevar este viaje a tu escenario o medio?
                </h3>
                <p class="text-white/70">
                    Escribime para coordinar fechas, sesiones acústicas, entrevistas o sincronizaciones.
                </p>
                <div class="flex flex-wrap justify-center gap-4">
                    <a
                        href="mailto:octavio.serrago@gmail.com"
                        class="inline-flex items-center gap-3 rounded-full bg-white px-6 py-3 text-sm font-semibold uppercase tracking-[0.3em] text-neutral-900 transition hover:bg-neutral-200"
                    >
                        Contactar
                        <i class="fa-solid fa-envelope text-xs" aria-hidden="true" />
                    </a>
                    <a
                        href="https://www.instagram.com/octavio.serrago"
                        target="_blank"
                        class="inline-flex items-center gap-3 rounded-full border border-white/40 px-6 py-3 text-sm font-semibold uppercase tracking-[0.3em] text-white transition hover:border-white hover:bg-white/10"
                    >
                        Instagram
                        <i class="fa-brands fa-instagram text-base" aria-hidden="true" />
                    </a>
                </div>
            </div>
        </section>

        <!-- Footer -->
        <Footer />
    </body>
</html>
